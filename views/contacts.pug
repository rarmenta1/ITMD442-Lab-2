extends layout

block content
  h1= title
  p List of Contacts
  hr
  if contacts.length > 0
    #contacts-wrapper 
    <table style="width:100%">
      <tr>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Email Address </th>
      </tr>
      each val in contacts 
        tr.contacts-list 
          th(data-contactid=val.id)= val.firstName
          th(data-contactid=val.id)= val.lastName
          th(data-contactid=val.id)= val.email
    </table>